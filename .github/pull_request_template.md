## What this changes

<!-- One or two sentences. -->

## Checklist

- [ ] `sh tests/test.sh` passes
- [ ] `shellcheck --shell=sh scripts/chime.sh tests/test.sh` is clean
- [ ] If `scripts/chime.sh` changed, `scripts/chime.ps1` changed the same way
- [ ] New behaviour has a test in both suites
- [ ] Regenerated sounds are committed, if `tools/gen_sounds.py` changed
